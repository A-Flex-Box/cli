package pkgs

// Default paths for CLI data files (relative to project root).

const (
	// DefaultHistoryPath is the default path to history.json.
	DefaultHistoryPath = "history/history.json"
	// DefaultHistoryMDPath is the default path to history.md.
	DefaultHistoryMDPath = "history/history.md"
)
